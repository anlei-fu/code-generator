<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- Jasmine code generator, a tool to build web crud application,with spring-
boot, mybatis, mysql,swagger,spring-security.
Generated at 2020-7-31 9:09:23 PM
All rights reserved by fuanlei(email:767550758@qq.com) since 2019 -->
<mapper namespace="com.jasmine.crawler.mapper.SystemAlarmMapper">
    <insert id="add" parameterType="com.jasmine.crawler.pojo.req.AddSystemAlarmReq">
          insert into system_alarm 
          <trim prefix="(" suffix=")" suffixOverrides=",">
             site_id,
             msg,
            <if test="isReaded != null">
                  is_readed,
            </if>
             create_time
          </trim>
          values
          <trim prefix="(" suffix=")" suffixOverrides=",">
             #{siteId},
             #{msg},
            <if test="isReaded != null">
                  #{isReaded},
            </if>
             current_timestamp
          </trim>
    </insert>
    <delete id="deleteById">
          delete from system_alarm t
          <where>
            t.id = #{id}
          </where>
    </delete>
    <update id="updateById" parameterType="com.jasmine.crawler.pojo.param.UpdateSystemAlarmParams">
          update system_alarm t
          <set>
            <if test="siteId != null and siteId != ''">
                  t.site_id = #{siteId},
            </if>
            <if test="msg != null and msg != ''">
                  t.msg = #{msg},
            </if>
            <if test="isReaded != null">
                  t.is_readed = #{isReaded},
            </if>
            <if test="id != null">
                  t.id = #{id}
            </if>
          </set>
          <where>
            t.id = #{id}
          </where>
    </update>
    <select id="getById" resultType="com.jasmine.crawler.pojo.entity.SystemAlarm">
          select
             t.id,
             t.site_id,
             t.msg,
             t.is_readed,
             t.create_time
          from system_alarm t
          <where>
            t.id = #{id}
          </where>
    </select>
    <select id="getPage" resultType="com.jasmine.crawler.pojo.entity.SystemAlarm" parameterType="com.jasmine.crawler.pojo.req.GetSystemAlarmPageReq">
          select
             t.id,
             t.site_id,
             t.msg,
             t.is_readed,
             t.create_time
          from system_alarm t
          <where>
            <if test="siteId != null">
                  t.site_id = #{siteId}
            </if>
            <if test="isReaded != null">
                  and t.is_readed = #{isReaded}
            </if>
            <if test="createTimeStart != null">
                  and  t.create_time >= #{createTimeStart}
            </if>
            <if test="createTimeEnd != null">
                  and t.create_time &lt; #{createTimeEnd}
            </if>
          </where>
    </select>
</mapper>
