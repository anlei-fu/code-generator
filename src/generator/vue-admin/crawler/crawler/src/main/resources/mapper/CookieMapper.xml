<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- Jasmine code generator, a tool to build web crud application,with spring-
boot, mybatis, mysql,swagger,spring-security.
Generated at 2020-7-31 9:09:23 PM
All rights reserved by fuanlei(email:767550758@qq.com) since 2019 -->
<mapper namespace="com.jasmine.crawler.mapper.CookieMapper">
    <insert id="add" parameterType="com.jasmine.crawler.pojo.req.AddCookieReq">
          insert into cookie 
          <trim prefix="(" suffix=")" suffixOverrides=",">
             site_id,
             site_account_id,
             cookie,
            <if test="loginIp != null and loginIp != ''">
                  login_ip,
            </if>
             create_time
          </trim>
          values
          <trim prefix="(" suffix=")" suffixOverrides=",">
             #{siteId},
             #{siteAccountId},
             #{cookie},
            <if test="loginIp != null and loginIp != ''">
                  #{loginIp},
            </if>
             current_timestamp
          </trim>
    </insert>
    <delete id="deleteById">
          delete from cookie t
          <where>
            t.id = #{id}
          </where>
    </delete>
    <update id="updateById" parameterType="com.jasmine.crawler.pojo.param.UpdateCookieParams">
          update cookie t
          <set>
            <if test="siteId != null">
                  t.site_id = #{siteId},
            </if>
            <if test="siteAccountId != null">
                  t.site_account_id = #{siteAccountId},
            </if>
            <if test="cookie != null and cookie != ''">
                  t.cookie = #{cookie},
            </if>
            <if test="loginIp != null and loginIp != ''">
                  t.login_ip = #{loginIp},
            </if>
            <if test="currentBlockCount != null">
                  t.current_block_count = #{currentBlockCount},
            </if>
            <if test="currentUseCount != null">
                  t.current_use_count = #{currentUseCount},
            </if>
            <if test="expireTime != null">
                  t.expire_time = #{expireTime},
            </if>
            <if test="id != null">
                  t.id = #{id}
            </if>
          </set>
          <where>
            t.id = #{id}
          </where>
    </update>
    <select id="getById" resultType="com.jasmine.crawler.pojo.entity.Cookie">
          select
             t.id,
             t.site_id,
             t.site_account_id,
             t.cookie,
             t.login_ip,
             t.current_block_count,
             t.current_use_count,
             t.expire_time,
             t.create_time
          from cookie t
          <where>
            t.id = #{id}
          </where>
    </select>
    <select id="getPage" resultType="com.jasmine.crawler.pojo.entity.Cookie" parameterType="com.jasmine.crawler.pojo.req.GetCookiePageReq">
          select
             t.id,
             t.site_id,
             t.site_account_id,
             t.cookie,
             t.login_ip,
             t.current_block_count,
             t.current_use_count,
             t.expire_time,
             t.create_time
          from cookie t
          <where>
            <if test="siteId != null">
                  t.site_id = #{siteId}
            </if>
            <if test="siteAccountId != null">
                  and t.site_account_id = #{siteAccountId}
            </if>
            <if test="createTimeStart != null">
                  and  t.create_time >= #{createTimeStart}
            </if>
            <if test="createTimeEnd != null">
                  and t.create_time &lt; #{createTimeEnd}
            </if>
          </where>
    </select>
</mapper>
