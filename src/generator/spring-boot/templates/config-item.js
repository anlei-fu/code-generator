const { all } = require("./../db/all")
const { ConfigItemBuilder } = require("@libPath/builders/ConfigItemBuilder")

@tableInfo
exports.@snameConfig = {
        table: all.@sname,
        name: "@name",
                items: [
@content                        
 ]
}