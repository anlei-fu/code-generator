const { all } = require("./../db/all")
const { ConfigItemBuilder } = require("./../../../builders/ConfigItemBuilder")

@tableInfo
exports.@snameConfig = {
        table: all.@sname,
        name: "@name",
                items: [
@content                        
 ]
}