exports.MasterNodeInfo = class {
        constructor () {
                // rest service host ip:port
                this.restServiceHost = "";

                // file service host ip:port
                this.fileSystemHost = "";

                // git service host  ip:port
                this.gitServiceHost = "";
        }
}