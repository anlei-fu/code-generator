exports.generateParam = (config) => {
 

   if(config.fields.length==1){

   }else{
        if(!config.params.name){

        }

        if(config.page){

        }

        if(config.time){

        }
   }
}

function doWrite(){
        
}