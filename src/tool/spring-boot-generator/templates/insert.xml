<insert id="@id" parameterType="com.@project.pojo.dao.@nameDao">
        insert  into @tableName
        <trim prefix="(" suffix=")" suffixOverrides=",">
              @columns
        </trim>
         values
         <trim prefix="(" suffix=")" suffixOverrides=",">
              @properties
          </trim>
</insert>