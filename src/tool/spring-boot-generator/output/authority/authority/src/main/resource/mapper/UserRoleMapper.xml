<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.authority.mapper.UserRoleMapper">
    <insert id="addUserRole">
          insert  into user_role
          <trim prefix="(" suffix=")" suffixOverrides=",">
 user_role.create_user,
 user_role.system_id,
 user_role.user_account,

          </trim>
          values
          <trim prefix="(" suffix=")" suffixOverrides=",">
 #{createUser} ,
 #{systemId} ,
 #{userAccount} ,

          </trim>
    </insert>
    <delete id="deleteUserRoleById">
          delete from user_role
@where
    </delete>
    <update id="updateUserRoleById">
        update user_role
@set
@where
    </update>
    <select id="getUserRoleById" resultType="com.authority.pojo.@resultType">
          select
undefined
          form userRoleName

@where
    </select>
    <select id="getUserRoleList" resultType="com.authority.pojo.@resultType">
          select
undefined
          form userRoleName

@where
    </select>

</mapper>