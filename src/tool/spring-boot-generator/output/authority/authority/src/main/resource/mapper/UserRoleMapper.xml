<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.authority.mapper.UserRoleMapper">
    <insert id="addUserRole">
          insert  into @tableName
          <trimprefix="("suffix=")" suffixOverrides=",">
undefinedundefinedundefined
          </trim>
          values
          <trimprefix="("suffix=")" suffixOverrides=",">
@content
          </trim>
    </insert>    <delete id="deleteUserRoleById">
          delete from @tableName
@where
    </delete>    <update id="updateUserRoleById">
        update @tableName
@set
@where
    </update>    <select id="getUserRoleById" resultType="com.authority.pojo.@resultType">
          select
undefined
          form userRoleName

@where
    </select>    <select id="getUserRoleList" resultType="com.authority.pojo.@resultType">
          select
undefined
          form userRoleName

@where
    </select>
</mapper>