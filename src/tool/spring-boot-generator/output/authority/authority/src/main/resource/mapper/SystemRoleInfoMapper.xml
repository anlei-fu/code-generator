<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.authority.mapper.SystemRoleInfoMapper">
    <insert id="addSystemRoleInfo">
          insert  into system_role_info
          <trim prefix="(" suffix=")" suffixOverrides=",">
 system_role_info.create_user,
 system_role_info.description,
 system_role_info.role,
 system_role_info.system_id,

          </trim>
          values
          <trim prefix="(" suffix=")" suffixOverrides=",">
 #{createUser} ,
 #{description} ,
 #{role} ,
 #{systemId} ,

          </trim>
    </insert>
    <delete id="deleteSystemRoleInfoById">
          delete from system_role_info
@where
    </delete>
    <update id="updateSystemRoleInfoById">
        update system_role_info
@set
@where
    </update>
    <select id="getSystemRoleInfoById" resultType="com.authority.pojo.@resultType">
          select
undefined
          form systemRoleInfoName

@where
    </select>
    <select id="getSystemRoleInfoList" resultType="com.authority.pojo.@resultType">
          select
undefined
          form systemRoleInfoName

@where
    </select>

</mapper>