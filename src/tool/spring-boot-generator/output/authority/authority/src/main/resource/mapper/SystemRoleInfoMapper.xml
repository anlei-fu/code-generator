<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.authority.mapper.SystemRoleInfoMapper">
    <insert id="addSystemRoleInfo">
          insert  into @tableName
          <trimprefix="("suffix=")" suffixOverrides=",">
undefinedundefinedundefinedundefined
          </trim>
          values
          <trimprefix="("suffix=")" suffixOverrides=",">
@content
          </trim>
    </insert>    <delete id="deleteSystemRoleInfoById">
          delete from @tableName
@where
    </delete>    <update id="updateSystemRoleInfoById">
        update @tableName
@set
@where
    </update>    <select id="getSystemRoleInfoById" resultType="com.authority.pojo.@resultType">
          select
undefined
          form systemRoleInfoName

@where
    </select>    <select id="getSystemRoleInfoList" resultType="com.authority.pojo.@resultType">
          select
undefined
          form systemRoleInfoName

@where
    </select>
</mapper>