<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.authority.mapper.UserInfoMapper">
    <insert id="addUserInfo">
          insert  into user_info
          <trim prefix="(" suffix=")" suffixOverrides=",">
 user_info.create_user,
 user_info.password,
 user_info.user_role,

          </trim>
          values
          <trim prefix="(" suffix=")" suffixOverrides=",">
 #{createUser} ,
 #{password} ,
 #{userRole} ,

          </trim>
    </insert>
    <delete id="deleteUserInfoByAccount">
          delete from user_info
@where
    </delete>
    <update id="updateUserInfoByAccount">
        update user_info
@set
@where
    </update>
    <select id="getUserInfoByAccount" resultType="com.authority.pojo.@resultType">
          select
undefined
          form userInfoName

@where
    </select>
    <select id="getUserInfoList" resultType="com.authority.pojo.@resultType">
          select
undefined
          form userInfoName

@where
    </select>

</mapper>