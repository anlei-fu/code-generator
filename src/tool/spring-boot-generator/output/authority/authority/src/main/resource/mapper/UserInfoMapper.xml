<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.authority.mapper.UserInfoMapper">
    <insert id="addUserInfo">
          insert  into @tableName
          <trimprefix="("suffix=")" suffixOverrides=",">
undefinedundefinedundefined
          </trim>
          values
          <trimprefix="("suffix=")" suffixOverrides=",">
@content
          </trim>
    </insert>    <delete id="deleteUserInfoByAccount">
          delete from @tableName
@where
    </delete>    <update id="updateUserInfoByAccount">
        update @tableName
@set
@where
    </update>    <select id="getUserInfoByAccount" resultType="com.authority.pojo.@resultType">
          select
undefined
          form userInfoName

@where
    </select>    <select id="getUserInfoList" resultType="com.authority.pojo.@resultType">
          select
undefined
          form userInfoName

@where
    </select>
</mapper>