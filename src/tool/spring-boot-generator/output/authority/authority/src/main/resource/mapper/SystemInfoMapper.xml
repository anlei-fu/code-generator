<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.authority.mapper.SystemInfoMapper">
    <insert id="addSystemInfo">
          insert  into @tableName
          <trim prefix="(" suffix=")" suffixOverrides=",">
undefinedundefinedundefined
          </trim>
          values
          <trim prefix="(" suffix=")" suffixOverrides=",">
@content
          </trim>
    </insert>
    <delete id="deleteSystemInfoById">
          delete from @tableName
@where
    </delete>
    <update id="updateSystemInfoById">
        update @tableName
@set
@where
    </update>
    <select id="getSystemInfoById" resultType="com.authority.pojo.@resultType">
          select
undefined
          form systemInfoName

@where
    </select>
    <select id="getSystemInfoList" resultType="com.authority.pojo.@resultType">
          select
undefined
          form systemInfoName

@where
    </select>

</mapper>