<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.authority.mapper.GroupRoleMapper">
    <insert id="addGroupRole">
          insert  into @tableName
          <trimprefix="("suffix=")" suffixOverrides=",">
undefinedundefinedundefinedundefined
          </trim>
          values
          <trimprefix="("suffix=")" suffixOverrides=",">
@content
          </trim>
    </insert>    <delete id="deleteGroupRoleById">
          delete from @tableName
@where
    </delete>    <update id="updateGroupRoleById">
        update @tableName
@set
@where
    </update>    <select id="getGroupRoleById" resultType="com.authority.pojo.@resultType">
          select
undefined
          form groupRoleName

@where
    </select>    <select id="getGroupRoleList" resultType="com.authority.pojo.@resultType">
          select
undefined
          form groupRoleName

@where
    </select>
</mapper>