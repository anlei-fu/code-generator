<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.authority.mapper.GroupRoleMapper">
    <insert id="addGroupRole">
          insert  into group_role
          <trim prefix="(" suffix=")" suffixOverrides=",">
 group_role.create_user,
 group_role.role,
 group_role.system_id,
 group_role.user_account,

          </trim>
          values
          <trim prefix="(" suffix=")" suffixOverrides=",">
 #{createUser} ,
 #{role} ,
 #{systemId} ,
 #{userAccount} ,

          </trim>
    </insert>
    <delete id="deleteGroupRoleById">
          delete from group_role
@where
    </delete>
    <update id="updateGroupRoleById">
        update group_role
@set
@where
    </update>
    <select id="getGroupRoleById" resultType="com.authority.pojo.@resultType">
          select
undefined
          form groupRoleName

@where
    </select>
    <select id="getGroupRoleList" resultType="com.authority.pojo.@resultType">
          select
undefined
          form groupRoleName

@where
    </select>

</mapper>