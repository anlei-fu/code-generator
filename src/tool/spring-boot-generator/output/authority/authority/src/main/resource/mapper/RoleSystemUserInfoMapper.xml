<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.authority.mapper.RoleSystemUserInfoMapper">
    <insert id="addRoleSystemUserInfo">
          insert  into @tableName
          <trimprefix="("suffix=")" suffixOverrides=",">
undefinedundefined
          </trim>
          values
          <trimprefix="("suffix=")" suffixOverrides=",">
@content
          </trim>
    </insert>    <delete id="deleteRoleSystemUserInfoByAccount">
          delete from @tableName
@where
    </delete>    <update id="updateRoleSystemUserInfoByAccount">
        update @tableName
@set
@where
    </update>    <select id="getRoleSystemUserInfoByAccount" resultType="com.authority.pojo.@resultType">
          select
undefined
          form roleSystemUserInfoName

@where
    </select>    <select id="getRoleSystemUserInfoList" resultType="com.authority.pojo.@resultType">
          select
undefined
          form roleSystemUserInfoName

@where
    </select>
</mapper>