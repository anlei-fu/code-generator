<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.authority.mapper.RoleSystemUserInfoMapper">
    <insert id="addRoleSystemUserInfo">
          insert  into role_system_user_info
          <trim prefix="(" suffix=")" suffixOverrides=",">
 role_system_user_info.account,
 role_system_user_info.password,
 role_system_user_info.role,

          </trim>
          values
          <trim prefix="(" suffix=")" suffixOverrides=",">
 #{account} ,
 #{password} ,
 #{role} ,

          </trim>
    </insert>
    <delete id="deleteRoleSystemUserInfoByAccount">
          delete from role_system_user_info
@where
    </delete>
    <update id="updateRoleSystemUserInfoByAccount">
        update role_system_user_info
@set
@where
    </update>
    <select id="getRoleSystemUserInfoByAccount" resultType="com.authority.pojo.@resultType">
          select
undefined
          form roleSystemUserInfoName

@where
    </select>
    <select id="getRoleSystemUserInfoList" resultType="com.authority.pojo.@resultType">
          select
undefined
          form roleSystemUserInfoName

@where
    </select>

</mapper>