<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.authority.mapper.SystemRoleGroupInfoMapper">
    <insert id="addSystemRoleGroupInfo">
          insert  into @tableName
          <trim prefix="(" suffix=")" suffixOverrides=",">
undefinedundefinedundefinedundefined
          </trim>
          values
          <trim prefix="(" suffix=")" suffixOverrides=",">
@content
          </trim>
    </insert>
    <delete id="deleteSystemRoleGroupInfoById">
          delete from @tableName
@where
    </delete>
    <update id="updateSystemRoleGroupInfoById">
        update @tableName
@set
@where
    </update>
    <select id="getSystemRoleGroupInfoById" resultType="com.authority.pojo.@resultType">
          select
undefined
          form systemRoleGroupInfoName

@where
    </select>
    <select id="getSystemRoleGroupInfoList" resultType="com.authority.pojo.@resultType">
          select
undefined
          form systemRoleGroupInfoName

@where
    </select>

</mapper>