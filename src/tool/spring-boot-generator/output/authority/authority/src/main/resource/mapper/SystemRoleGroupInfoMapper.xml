<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.authority.mapper.SystemRoleGroupInfoMapper">
    <insert id="addSystemRoleGroupInfo">
          insert  into system_role_group_info
          <trim prefix="(" suffix=")" suffixOverrides=",">
 system_role_group_info.create_user,
 system_role_group_info.description,
 system_role_group_info.role,
 system_role_group_info.system_id,

          </trim>
          values
          <trim prefix="(" suffix=")" suffixOverrides=",">
 #{createUser} ,
 #{description} ,
 #{role} ,
 #{systemId} ,

          </trim>
    </insert>
    <delete id="deleteSystemRoleGroupInfoById">
          delete from system_role_group_info
@where
    </delete>
    <update id="updateSystemRoleGroupInfoById">
        update system_role_group_info
@set
@where
    </update>
    <select id="getSystemRoleGroupInfoById" resultType="com.authority.pojo.@resultType">
          select
undefined
          form systemRoleGroupInfoName

@where
    </select>
    <select id="getSystemRoleGroupInfoList" resultType="com.authority.pojo.@resultType">
          select
undefined
          form systemRoleGroupInfoName

@where
    </select>

</mapper>