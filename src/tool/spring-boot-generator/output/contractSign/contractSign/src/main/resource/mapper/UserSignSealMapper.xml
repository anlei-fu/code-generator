<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.contractSign.mapper.@nameMapper">
    <insert id="addUserSignSeal">
          insert  into @tableName
          <trimprefix="("suffix=")" suffixOverrides=",">
undefinedundefinedundefinedundefinedundefined
          </trim>
          values
          <trimprefix="("suffix=")" suffixOverrides=",">
@content
          </trim>
    </insert>    <delete id="deleteUserSignSealById">
          delete from @tableName
@where
    </delete>    <update id="updateUserSignSealById">
        update @tableName
@set
@where
    </update>    <select id="getUserSignSealById" resultType="com.contractSign.pojo.@resultType">
          select
undefined
          form userSignSealName

@where
    </select>    <select id="getUserSignSealList" resultType="com.contractSign.pojo.@resultType">
          select
undefined
          form userSignSealName

@where
    </select>
</mapper>