<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.contractSign.mapper.@nameMapper">
    <insert id="addUserWallet">
          insert  into @tableName
          <trimprefix="("suffix=")" suffixOverrides=",">
undefinedundefinedundefined
          </trim>
          values
          <trimprefix="("suffix=")" suffixOverrides=",">
@content
          </trim>
    </insert>    <delete id="deleteUserWalletById">
          delete from @tableName
@where
    </delete>    <update id="updateUserWalletById">
        update @tableName
@set
@where
    </update>    <select id="getUserWalletById" resultType="com.contractSign.pojo.@resultType">
          select
undefined
          form userWalletName

@where
    </select>    <select id="getUserWalletList" resultType="com.contractSign.pojo.@resultType">
          select
undefined
          form userWalletName

@where
    </select>
</mapper>