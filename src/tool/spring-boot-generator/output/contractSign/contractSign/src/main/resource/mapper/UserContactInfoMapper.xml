<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.contractSign.mapper.@nameMapper">
    <insert id="addUserContactInfo">
          insert  into @tableName
          <trimprefix="("suffix=")" suffixOverrides=",">
undefinedundefinedundefinedundefined
          </trim>
          values
          <trimprefix="("suffix=")" suffixOverrides=",">
@content
          </trim>
    </insert>    <delete id="deleteUserContactInfoById">
          delete from @tableName
@where
    </delete>    <update id="updateUserContactInfoById">
        update @tableName
@set
@where
    </update>    <select id="getUserContactInfoById" resultType="com.contractSign.pojo.@resultType">
          select
undefined
          form userContactInfoName

@where
    </select>    <select id="getUserContactInfoList" resultType="com.contractSign.pojo.@resultType">
          select
undefined
          form userContactInfoName

@where
    </select>
</mapper>