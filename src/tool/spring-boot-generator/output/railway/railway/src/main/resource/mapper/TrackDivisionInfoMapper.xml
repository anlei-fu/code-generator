<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.railway.mapper.TrackDivisionInfoMapper">
    <insert id="addTrackDivisionInfo" parameterType="com.railway.pojo.req.AddTrackDivisionInfoReq">
          insert  into track_division_info
          <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="bureauId != null">
                  track_division_info.bureau_id,
            </if>
             track_division_info.full_name,
            <if test="inputCode != null">
                  track_division_info.input_code,
            </if>
             track_division_info.is_delete,
            <if test="operator != null">
                  track_division_info.operator,
            </if>
            <if test="shortName != null">
                  track_division_info.short_name,
            </if>
             track_division_info.create_time
          </trim>
          values
          <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="bureauId != null">
                  #{bureauId},
            </if>
             #{fullName},
            <if test="inputCode != null">
                  #{inputCode},
            </if>
             #{isDelete},
            <if test="operator != null">
                  #{operator},
            </if>
            <if test="shortName != null">
                  #{shortName},
            </if>
             current_timestamp
          </trim>
    </insert>
    <delete id="deleteTrackDivisionInfoById">
          delete from track_division_info
          <where>
            <if test="id != null">
                  track_division_info.id = #{id}
            </if>
          </where>
    </delete>
    <update id="updateTrackDivisionInfoById" parameterType="com.railway.pojo.param.UpdateTrackDivisionInfoByIdParams">
        update track_division_info
        <set>
            <if test="bureauId != null">
                  track_division_info.bureau_id = #{BureauId},
            </if>
            <if test="fullName != null">
                  track_division_info.full_name = #{FullName},
            </if>
            <if test="inputCode != null">
                  track_division_info.input_code = #{InputCode},
            </if>
            <if test="isDelete != null">
                  track_division_info.is_delete = #{IsDelete},
            </if>
            <if test="operator != null">
                  track_division_info.operator = #{Operator},
            </if>
            <if test="shortName != null">
                  track_division_info.short_name = #{ShortName}
            </if>
            track_division_info.update_time= current_timestamp
        </set>
          <where>
            <if test="id != null">
                  track_division_info.id = #{id}
            </if>
          </where>
    </update>
    <select id="getTrackDivisionInfoById" resultType="com.railway.pojo.entity.TrackDivisionInfo">
          select
             track_division_info.bureau_id,
             track_division_info.create_time,
             track_division_info.full_name,
             track_division_info.id,
             track_division_info.input_code,
             track_division_info.is_delete,
             track_division_info.operator,
             track_division_info.short_name,
             track_division_info.update_time
          form track_division_info
          <where>
            <if test="id != null">
                  track_division_info.id = #{id}
            </if>
          </where>
    </select>
    <select id="getTrackDivisionInfoList" resultType="com.railway.pojo.entity.TrackDivisionInfo" parameterType="com.railway.pojo.req.GetTrackDivisionInfoListReq">
          select
             track_division_info.bureau_id,
             track_division_info.create_time,
             track_division_info.full_name,
             track_division_info.id,
             track_division_info.input_code,
             track_division_info.is_delete,
             track_division_info.operator,
             track_division_info.short_name,
             track_division_info.update_time
          form track_division_info
          <where>
            <if test="bureauId != null">
                  track_division_info.bureau_id = #{bureauId}
            </if>
            <if test="createTimeStart !=null and  createTimeEnd != null">
                  and track_division_info.create_time between #{createTimeStart} and #{createTimeEnd}
            </if>
            <if test="inputCode != null and inputCode != ''">
                  and track_division_info.input_code = #{inputCode}
            </if>
            <if test="isDelete != null">
                  and track_division_info.is_delete = #{isDelete}
            </if>
            <if test="operator != null">
                  and track_division_info.operator = #{operator}
            </if>
            <if test="updateTimeStart !=null and  updateTimeEnd != null">
                  and track_division_info.update_time between #{updateTimeStart} and #{updateTimeEnd}
            </if>
          </where>
    </select>
</mapper>
