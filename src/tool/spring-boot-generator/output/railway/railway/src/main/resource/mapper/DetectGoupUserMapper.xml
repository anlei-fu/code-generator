<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.railway.mapper.DetectGoupUserMapper">
    <insert id="addDetectGoupUser" parameterType="com.railway.pojo.req.AddDetectGoupUserReq">
          insert  into detect_goup_user
          <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="groupId != null">
                  detect_goup_user.group_id,
            </if>
             detect_goup_user.is_delete,
            <if test="memberId != null">
                  detect_goup_user.member_id,
            </if>
            <if test="operator != null">
                  detect_goup_user.operator,
            </if>
            <if test="status != null">
                  detect_goup_user.status,
            </if>
             detect_goup_user.create_time
          </trim>
          values
          <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="groupId != null">
                  #{groupId},
            </if>
             #{isDelete},
            <if test="memberId != null">
                  #{memberId},
            </if>
            <if test="operator != null">
                  #{operator},
            </if>
            <if test="status != null">
                  #{status},
            </if>
             current_timestamp
          </trim>
    </insert>
    <delete id="deleteDetectGoupUserById">
          delete from detect_goup_user
          <where>
            <if test="id != null">
                  detect_goup_user.id = #{id}
            </if>
          </where>
    </delete>
    <update id="updateDetectGoupUserById" parameterType="com.railway.pojo.param.UpdateDetectGoupUserByIdParams">
        update detect_goup_user
        <set>
            <if test="groupId != null">
                  detect_goup_user.group_id = #{GroupId},
            </if>
            <if test="isDelete != null">
                  detect_goup_user.is_delete = #{IsDelete},
            </if>
            <if test="memberId != null">
                  detect_goup_user.member_id = #{MemberId},
            </if>
            <if test="operator != null">
                  detect_goup_user.operator = #{Operator},
            </if>
            <if test="status != null">
                  detect_goup_user.status = #{Status}
            </if>
            detect_goup_user.update_time= current_timestamp
        </set>
          <where>
            <if test="id != null">
                  detect_goup_user.id = #{id}
            </if>
          </where>
    </update>
    <select id="getDetectGoupUserById" resultType="com.railway.pojo.entity.DetectGoupUser">
          select
             detect_goup_user.create_time,
             detect_goup_user.group_id,
             detect_goup_user.id,
             detect_goup_user.is_delete,
             detect_goup_user.member_id,
             detect_goup_user.operator,
             detect_goup_user.status,
             detect_goup_user.update_time
          form detect_goup_user
          <where>
            <if test="id != null">
                  detect_goup_user.id = #{id}
            </if>
          </where>
    </select>
    <select id="getDetectGoupUserList" resultType="com.railway.pojo.entity.DetectGoupUser" parameterType="com.railway.pojo.req.GetDetectGoupUserListReq">
          select
             detect_goup_user.create_time,
             detect_goup_user.group_id,
             detect_goup_user.id,
             detect_goup_user.is_delete,
             detect_goup_user.member_id,
             detect_goup_user.operator,
             detect_goup_user.status,
             detect_goup_user.update_time
          form detect_goup_user
          <where>
            <if test="createTimeStart !=null and  createTimeEnd != null">
                  detect_goup_user.create_time between #{createTimeStart} and #{createTimeEnd}
            </if>
            <if test="groupId != null">
                  and detect_goup_user.group_id = #{groupId}
            </if>
            <if test="isDelete != null">
                  and detect_goup_user.is_delete = #{isDelete}
            </if>
            <if test="memberId != null">
                  and detect_goup_user.member_id = #{memberId}
            </if>
            <if test="operator != null">
                  and detect_goup_user.operator = #{operator}
            </if>
            <if test="status != null">
                  and detect_goup_user.status = #{status}
            </if>
            <if test="updateTimeStart !=null and  updateTimeEnd != null">
                  and detect_goup_user.update_time between #{updateTimeStart} and #{updateTimeEnd}
            </if>
          </where>
    </select>
</mapper>
