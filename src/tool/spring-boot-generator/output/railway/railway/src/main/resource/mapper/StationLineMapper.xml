<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.railway.mapper.StationLineMapper">
    <insert id="addStationLine" parameterType="com.railway.pojo.req.AddStationLineReq">
          insert  into station_line 
          <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="centDistance != null">
                  station_line.cent_distance,
            </if>
            <if test="direction != null">
                  station_line.direction,
            </if>
            <if test="endDistance != null">
                  station_line.end_distance,
            </if>
             station_line.is_delete,
             station_line.line_id,
            <if test="operator != null">
                  station_line.operator,
            </if>
            <if test="sortNum != null">
                  station_line.sort_num,
            </if>
            <if test="startDistance != null">
                  station_line.start_distance,
            </if>
             station_line.station_id,
            <if test="status != null">
                  station_line.status,
            </if>
             station_line.create_time
          </trim>
          values
          <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="centDistance != null">
                  #{centDistance},
            </if>
            <if test="direction != null">
                  #{direction},
            </if>
            <if test="endDistance != null">
                  #{endDistance},
            </if>
             #{isDelete},
             #{lineId},
            <if test="operator != null">
                  #{operator},
            </if>
            <if test="sortNum != null">
                  #{sortNum},
            </if>
            <if test="startDistance != null">
                  #{startDistance},
            </if>
             #{stationId},
            <if test="status != null">
                  #{status},
            </if>
             current_timestamp
          </trim>
    </insert>
    <delete id="deleteStationLineById">
          delete from station_line 
          <where>
            <if test="id != null">
                  station_line.id = #{id}
            </if>
          </where>
    </delete>
    <update id="updateStationLineById" parameterType="com.railway.pojo.param.UpdateStationLineByIdParams">
        update station_line 
        <set>
            <if test="centDistance != null">
                  station_line.cent_distance = #{CentDistance},
            </if>
            <if test="direction != null">
                  station_line.direction = #{Direction},
            </if>
            <if test="endDistance != null">
                  station_line.end_distance = #{EndDistance},
            </if>
            <if test="isDelete != null">
                  station_line.is_delete = #{IsDelete},
            </if>
            <if test="lineId != null">
                  station_line.line_id = #{LineId},
            </if>
            <if test="operator != null">
                  station_line.operator = #{Operator},
            </if>
            <if test="sortNum != null">
                  station_line.sort_num = #{SortNum},
            </if>
            <if test="startDistance != null">
                  station_line.start_distance = #{StartDistance},
            </if>
            <if test="stationId != null">
                  station_line.station_id = #{StationId},
            </if>
            <if test="status != null">
                  station_line.status = #{Status}
            </if>
            station_line.update_time= current_timestamp
        </set>
          <where>
            <if test="id != null">
                  station_line.id = #{id}
            </if>
          </where>
    </update>
    <select id="getStationLineById" resultType="com.railway.pojo.entity.StationLine">
          select
             station_line.cent_distance,
             station_line.create_time,
             station_line.direction,
             station_line.end_distance,
             station_line.id,
             station_line.is_delete,
             station_line.line_id,
             station_line.operator,
             station_line.sort_num,
             station_line.start_distance,
             station_line.station_id,
             station_line.status,
             station_line.update_time
          form station_line 
          <where>
            <if test="id != null">
                  station_line.id = #{id}
            </if>
          </where>
    </select>
    <select id="getStationLineList" resultType="com.railway.pojo.entity.StationLine" parameterType="com.railway.pojo.req.GetStationLineListReq">
          select
             station_line.cent_distance,
             station_line.create_time,
             station_line.direction,
             station_line.end_distance,
             station_line.id,
             station_line.is_delete,
             station_line.line_id,
             station_line.operator,
             station_line.sort_num,
             station_line.start_distance,
             station_line.station_id,
             station_line.status,
             station_line.update_time
          form station_line 
          <where>
            <if test="centDistance != null">
                  station_line.cent_distance = #{centDistance}
            </if>
            <if test="createTimeStart !=null and  createTimeEnd != null">
                  and station_line.create_time between #{createTimeStart} and #{createTimeEnd}
            </if>
            <if test="direction != null">
                  and station_line.direction = #{direction}
            </if>
            <if test="endDistance != null">
                  and station_line.end_distance = #{endDistance}
            </if>
            <if test="isDelete != null">
                  and station_line.is_delete = #{isDelete}
            </if>
            <if test="lineId != null">
                  and station_line.line_id = #{lineId}
            </if>
            <if test="operator != null">
                  and station_line.operator = #{operator}
            </if>
            <if test="sortNum != null and sortNum != ''">
                  and station_line.sort_num = #{sortNum}
            </if>
            <if test="startDistance != null">
                  and station_line.start_distance = #{startDistance}
            </if>
            <if test="stationId != null">
                  and station_line.station_id = #{stationId}
            </if>
            <if test="status != null">
                  and station_line.status = #{status}
            </if>
            <if test="updateTimeStart !=null and  updateTimeEnd != null">
                  and station_line.update_time between #{updateTimeStart} and #{updateTimeEnd}
            </if>
          </where>
    </select>
</mapper>
