<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.undefined.mapper.BureauInfoMapper">
     <insert id="addBureauInfo" parameterType="com.@project.pojo.dao.@nameDao">
          insert  into bureau_info
          <trim prefix="(" suffix=")" suffixOverrides=",">
          <if test="bureauCode != null">
            bureau_info.bureau_code,
          </if>
          <if test="bureauName != null">
            bureau_info.bureau_name,
          </if>
          <if test="createTime != null">
            bureau_info.create_time,
          </if>
          <if test="inputCode != null">
            bureau_info.input_code,
          </if>
          <if test="isDelete != null">
            bureau_info.is_delete,
          </if>
          <if test="operator != null">
            bureau_info.operator,
          </if>
          <if test="updateTime != null">
            bureau_info.update_time,
          </if>
          </trim>
          values
          <trim prefix="(" suffix=")" suffixOverrides=",">
          <if test="bureauCode != null">
            #{bureauCode},
          </if>
          <if test="bureauName != null">
            #{bureauName},
          </if>
          <if test="createTime != null">
            #{createTime},
          </if>
          <if test="inputCode != null">
            #{inputCode},
          </if>
          <if test="isDelete != null">
            #{isDelete},
          </if>
          <if test="operator != null">
            #{operator},
          </if>
          <if test="updateTime != null">
            #{updateTime},
          </if>
          </trim>
    </insert>

    <delete id="deleteBureauInfoById">
        delete from bureau_info
        <where>
          <if test="id != null">
            bureau_info.id = #{id}
          </if>
        </where>
    </delete>

    <update id="updateBureauInfoById">
      update bureau_info
        <set>
          <if test="bureauCode != null">
            bureau_info.bureau_code = #{bureauCode},
          </if>
          <if test="bureauName != null">
            bureau_info.bureau_name = #{bureauName},
          </if>
          <if test="createTime != null">
            bureau_info.create_time = #{createTime},
          </if>
          <if test="inputCode != null">
            bureau_info.input_code = #{inputCode},
          </if>
          <if test="isDelete != null">
            bureau_info.is_delete = #{isDelete},
          </if>
          <if test="operator != null">
            bureau_info.operator = #{operator},
          </if>
          <if test="updateTime != null">
            bureau_info.update_time = #{updateTime}
          </if>
        </set>
        <where>
          <if test="id != null">
            bureau_info.id = #{id}
          </if>
        </where>
    </update>

  <select id="getBureauInfoById">
        select
            bureau_info.bureau_code,
            bureau_info.bureau_name,
            bureau_info.create_time,
            bureau_info.id,
            bureau_info.input_code,
            bureau_info.is_delete,
            bureau_info.operator,
            bureau_info.update_time
        form bureau_info
                <where>
          <if test="id != null">
            bureau_info.id = #{id}
          </if>
        </where>
  </select>

  <select id="getBureauInfoList">
        select
            bureau_info.bureau_code,
            bureau_info.bureau_name,
            bureau_info.create_time,
            bureau_info.id,
            bureau_info.input_code,
            bureau_info.is_delete,
            bureau_info.operator,
            bureau_info.update_time
        form bureau_info
                <where>
          <if test="bureauCode != null">
            bureau_info.bureau_code = #{bureauCode}
          </if>
          <if test="bureauName != null">
            and bureau_info.bureau_name = #{bureauName}
          </if>
          <if test="createTime != null">
            and bureau_info.create_time = #{createTime}
          </if>
          <if test="inputCode != null">
            and bureau_info.input_code = #{inputCode}
          </if>
          <if test="isDelete != null">
            and bureau_info.is_delete = #{isDelete}
          </if>
          <if test="operator != null">
            and bureau_info.operator = #{operator}
          </if>
          <if test="updateTime != null">
            and bureau_info.update_time = #{updateTime}
          </if>
        </where>
  </select>
</mapper>