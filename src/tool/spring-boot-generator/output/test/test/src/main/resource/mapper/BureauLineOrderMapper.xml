<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.mapper.BureauLineOrderMapper">
    <insert id="addBureauLineOrder">
          insert  into bureau_line_order
          <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="bureauId != null">
              bureau_line_order.bureau_id,
            </if>
            <if test="createTime != null">
              bureau_line_order.create_time,
            </if>
            <if test="lineId != null">
              bureau_line_order.line_id,
            </if>
            <if test="operator != null">
              bureau_line_order.operator,
            </if>
            <if test="sortNum != null">
              bureau_line_order.sort_num,
            </if>
            <if test="updateTime != null">
              bureau_line_order.update_time,
            </if>
          </trim>
          values
          <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="bureauId != null">
              #{bureauId},
            </if>
            <if test="createTime != null">
              #{createTime},
            </if>
            <if test="lineId != null">
              #{lineId},
            </if>
            <if test="operator != null">
              #{operator},
            </if>
            <if test="sortNum != null">
              #{sortNum},
            </if>
            <if test="updateTime != null">
              #{updateTime},
            </if>
          </trim>
    </insert>

    <delete id="deleteBureauLineOrderById">
          delete from bureau_line_order
  where
          bureau_line_order.id = #{id}
    </delete>

    <update id="updateBureauLineOrderById">
        update bureau_line_order
        <set>
            <if test="bureauId != null">
              bureau_line_order.bureau_id = #{bureauId},
            </if>
            <if test="createTime != null">
              bureau_line_order.create_time = #{createTime},
            </if>
            <if test="lineId != null">
              bureau_line_order.line_id = #{lineId},
            </if>
            <if test="operator != null">
              bureau_line_order.operator = #{operator},
            </if>
            <if test="sortNum != null">
              bureau_line_order.sort_num = #{sortNum},
            </if>
            <if test="updateTime != null">
              bureau_line_order.update_time = #{updateTime}
            </if>
        </set>
  where
          bureau_line_order.id = #{id}
    </update>

    <select id="getBureauLineOrderById" resultType="com.@project.pojo.@resultType">
          select
              bureau_line_order.bureau_id,
              bureau_line_order.create_time,
              bureau_line_order.id,
              bureau_line_order.line_id,
              bureau_line_order.operator,
              bureau_line_order.sort_num,
              bureau_line_order.update_time
          form bureau_line_order
  where
          bureau_line_order.id = #{id}
    </select>

    <select id="getBureauLineOrderList" resultType="com.@project.pojo.@resultType">
          select
              bureau_line_order.bureau_id,
              bureau_line_order.create_time,
              bureau_line_order.id,
              bureau_line_order.line_id,
              bureau_line_order.operator,
              bureau_line_order.sort_num,
              bureau_line_order.update_time
          form bureau_line_order
          <where>
            <if test="bureauId != null">
              bureau_line_order.bureau_id = #{bureauId}
            </if>
            <if test="createTime != null">
              and bureau_line_order.create_time = #{createTime}
            </if>
            <if test="lineId != null">
              and bureau_line_order.line_id = #{lineId}
            </if>
            <if test="operator != null">
              and bureau_line_order.operator = #{operator}
            </if>
            <if test="sortNum != null">
              and bureau_line_order.sort_num = #{sortNum}
            </if>
            <if test="updateTime != null">
              and bureau_line_order.update_time = #{updateTime}
            </if>
          </where>
    </select>
</mapper>