<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.undefined.mapper.DivisionUserMapper">
     <insert id="addDivisionUser" parameterType="com.@project.pojo.dao.@nameDao">
          insert  into division_user
          <trim prefix="(" suffix=")" suffixOverrides=",">
          <if test="createTime != null">
            division_user.create_time,
          </if>
          <if test="divisionId != null">
            division_user.division_id,
          </if>
          <if test="isDelete != null">
            division_user.is_delete,
          </if>
          <if test="operator != null">
            division_user.operator,
          </if>
          <if test="updateTime != null">
            division_user.update_time,
          </if>
          <if test="userId != null">
            division_user.user_id,
          </if>
          </trim>
          values
          <trim prefix="(" suffix=")" suffixOverrides=",">
          <if test="createTime != null">
            #{createTime},
          </if>
          <if test="divisionId != null">
            #{divisionId},
          </if>
          <if test="isDelete != null">
            #{isDelete},
          </if>
          <if test="operator != null">
            #{operator},
          </if>
          <if test="updateTime != null">
            #{updateTime},
          </if>
          <if test="userId != null">
            #{userId},
          </if>
          </trim>
    </insert>

    <delete id="deleteDivisionUserById">
        delete from division_user
        <where>
          <if test="id != null">
            division_user.id = #{id}
          </if>
        </where>
    </delete>

    <update id="updateDivisionUserById">
      update division_user
        <set>
          <if test="createTime != null">
            division_user.create_time = #{createTime},
          </if>
          <if test="divisionId != null">
            division_user.division_id = #{divisionId},
          </if>
          <if test="isDelete != null">
            division_user.is_delete = #{isDelete},
          </if>
          <if test="operator != null">
            division_user.operator = #{operator},
          </if>
          <if test="updateTime != null">
            division_user.update_time = #{updateTime},
          </if>
          <if test="userId != null">
            division_user.user_id = #{userId}
          </if>
        </set>
        <where>
          <if test="id != null">
            division_user.id = #{id}
          </if>
        </where>
    </update>

  <select id="getDivisionUserById">
        select
            division_user.create_time,
            division_user.division_id,
            division_user.id,
            division_user.is_delete,
            division_user.operator,
            division_user.update_time,
            division_user.user_id
        form division_user
                <where>
          <if test="id != null">
            division_user.id = #{id}
          </if>
        </where>
  </select>

  <select id="getDivisionUserList">
        select
            division_user.create_time,
            division_user.division_id,
            division_user.id,
            division_user.is_delete,
            division_user.operator,
            division_user.update_time,
            division_user.user_id
        form division_user
                <where>
          <if test="createTime != null">
            division_user.create_time = #{createTime}
          </if>
          <if test="divisionId != null">
            and division_user.division_id = #{divisionId}
          </if>
          <if test="isDelete != null">
            and division_user.is_delete = #{isDelete}
          </if>
          <if test="operator != null">
            and division_user.operator = #{operator}
          </if>
          <if test="updateTime != null">
            and division_user.update_time = #{updateTime}
          </if>
          <if test="userId != null">
            and division_user.user_id = #{userId}
          </if>
        </where>
  </select>
</mapper>