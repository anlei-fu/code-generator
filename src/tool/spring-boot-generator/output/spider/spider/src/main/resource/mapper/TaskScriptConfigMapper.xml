<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.undefined.mapper.TaskScriptConfigMapper">
    <insert id="addTaskScriptConfig">
          insert  into task_script_config
          <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="createTime != null">
              task_script_config.create_time,
            </if>
            <if test="file != null">
              task_script_config.file,
            </if>
              task_script_config.task_config_main_id,
              task_script_config.version,
          </trim>
          values
          <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="createTime != null">
              #{createTime},
            </if>
            <if test="file != null">
              #{file},
            </if>
              #{taskConfigMainId},
              #{version},
          </trim>
    </insert>

    <delete id="deleteTaskScriptConfigById">
          delete from task_script_config
          <where>
            <if test="id != null">
              task_script_config.id = #{id}
            </if>
          </where>
    </delete>

    <update id="updateTaskScriptConfigById">
        update task_script_config
        <set>
            <if test="createTime != null">
              task_script_config.create_time = #{createTime},
            </if>
            <if test="file != null">
              task_script_config.file = #{file},
            </if>
            <if test="taskConfigMainId != null">
              task_script_config.task_config_main_id = #{taskConfigMainId},
            </if>
            <if test="version != null">
              task_script_config.version = #{version}
            </if>
        </set>
          <where>
            <if test="id != null">
              task_script_config.id = #{id}
            </if>
          </where>
    </update>

    <select id="getTaskScriptConfigById">
          select
              task_script_config.create_time,
              task_script_config.file,
              task_script_config.id,
              task_script_config.task_config_main_id,
              task_script_config.version
          form task_script_config
          <where>
            <if test="id != null">
              task_script_config.id = #{id}
            </if>
          </where>
    </select>

    <select id="getTaskScriptConfigList">
          select
              task_script_config.create_time,
              task_script_config.file,
              task_script_config.id,
              task_script_config.task_config_main_id,
              task_script_config.version
          form task_script_config
          <where>
            <if test="createTime != null">
              task_script_config.create_time = #{createTime}
            </if>
            <if test="file != null">
              and task_script_config.file = #{file}
            </if>
            <if test="taskConfigMainId != null">
              and task_script_config.task_config_main_id = #{taskConfigMainId}
            </if>
            <if test="version != null">
              and task_script_config.version = #{version}
            </if>
          </where>
    </select>
</mapper>