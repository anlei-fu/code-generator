<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spider.mapper.DomainModelMapper">
    <insert id="addDomainModel">
          insert  into @tableName
          <trimprefix="("suffix=")" suffixOverrides=",">
undefinedundefinedundefined
          </trim>
          values
          <trimprefix="("suffix=")" suffixOverrides=",">
@content
          </trim>
    </insert>    <delete id="deleteDomainModelById">
          delete from @tableName
@where
    </delete>    <update id="updateDomainModelById">
        update @tableName
@set
@where
    </update>    <select id="getDomainModelById" resultType="com.spider.pojo.@resultType">
          select
undefined
          form domainModelName

@where
    </select>    <select id="getDomainModelList" resultType="com.spider.pojo.@resultType">
          select
undefined
          form domainModelName

@where
    </select>
</mapper>