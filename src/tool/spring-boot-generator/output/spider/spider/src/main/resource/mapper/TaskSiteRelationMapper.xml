<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spider.mapper.TaskSiteRelationMapper">
    <insert id="addTaskSiteRelation">
          insert  into task_site_relation
          <trim prefix="(" suffix=")" suffixOverrides=",">
 task_site_relation.site_id,
 task_site_relation.task_id,
 task_site_relation.user_id,
 task_site_relation.domain,

          </trim>
          values
          <trim prefix="(" suffix=")" suffixOverrides=",">
 #{siteId} ,
 #{taskId} ,
 #{userId} ,
 #{domain} ,

          </trim>
    </insert>
    <delete id="deleteTaskSiteRelationById">
          delete from task_site_relation
@where
    </delete>
    <update id="updateTaskSiteRelationById">
        update task_site_relation
@set
@where
    </update>
    <select id="getTaskSiteRelationById" resultType="com.spider.pojo.@resultType">
          select
undefined
          form taskSiteRelationName

@where
    </select>
    <select id="getTaskSiteRelationList" resultType="com.spider.pojo.@resultType">
          select
undefined
          form taskSiteRelationName

@where
    </select>

</mapper>