<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spider.mapper.UserInfoMapper">
    <insert id="addUserInfo">
          insert  into user_info
          <trim prefix="(" suffix=")" suffixOverrides=",">
 user_info.phone_no,
 user_info.email,
 user_info.password,
 user_info.last_login_ip,
 user_info.last_login_time,
 user_info.create_time,

          </trim>
          values
          <trim prefix="(" suffix=")" suffixOverrides=",">
 #{phoneNo} ,
 #{email} ,
 #{password} ,
 #{lastLoginIp} ,
 #{lastLoginTime} ,
 #{createTime} ,

          </trim>
    </insert>
    <delete id="deleteUserInfoById">
          delete from user_info
@where
    </delete>
    <update id="updateUserInfoById">
        update user_info
@set
@where
    </update>
    <select id="getUserInfoById" resultType="com.spider.pojo.@resultType">
          select
undefined
          form userInfoName

@where
    </select>
    <select id="getUserInfoList" resultType="com.spider.pojo.@resultType">
          select
undefined
          form userInfoName

@where
    </select>

</mapper>