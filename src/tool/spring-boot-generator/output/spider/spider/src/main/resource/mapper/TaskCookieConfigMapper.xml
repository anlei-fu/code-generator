<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spider.mapper.TaskCookieConfigMapper">
    <insert id="addTaskCookieConfig">
          insert  into task_cookie_config
          <trim prefix="(" suffix=")" suffixOverrides=",">
 task_cookie_config.task_config_main_id,
 task_cookie_config.domain_id,
 task_cookie_config.key,
 task_cookie_config.value,
 task_cookie_config.expiration,
 task_cookie_config.create_time,

          </trim>
          values
          <trim prefix="(" suffix=")" suffixOverrides=",">
 #{taskConfigMainId} ,
 #{domainId} ,
 #{key} ,
 #{value} ,
 #{expiration} ,
 #{createTime} ,

          </trim>
    </insert>
    <delete id="deleteTaskCookieConfigById">
          delete from task_cookie_config
@where
    </delete>
    <update id="updateTaskCookieConfigById">
        update task_cookie_config
@set
@where
    </update>
    <select id="getTaskCookieConfigById" resultType="com.spider.pojo.@resultType">
          select
undefined
          form taskCookieConfigName

@where
    </select>
    <select id="getTaskCookieConfigList" resultType="com.spider.pojo.@resultType">
          select
undefined
          form taskCookieConfigName

@where
    </select>

</mapper>