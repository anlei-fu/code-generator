<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spider.mapper.UserWalletMapper">
    <insert id="addUserWallet">
          insert  into user_wallet
          <trim prefix="(" suffix=")" suffixOverrides=",">
 user_wallet.user_id,
 user_wallet.total,
 user_wallet.create_time,

          </trim>
          values
          <trim prefix="(" suffix=")" suffixOverrides=",">
 #{userId} ,
 #{total} ,
 #{createTime} ,

          </trim>
    </insert>
    <delete id="deleteUserWalletById">
          delete from user_wallet
@where
    </delete>
    <update id="updateUserWalletById">
        update user_wallet
@set
@where
    </update>
    <select id="getUserWalletById" resultType="com.spider.pojo.@resultType">
          select
undefined
          form userWalletName

@where
    </select>
    <select id="getUserWalletList" resultType="com.spider.pojo.@resultType">
          select
undefined
          form userWalletName

@where
    </select>

</mapper>