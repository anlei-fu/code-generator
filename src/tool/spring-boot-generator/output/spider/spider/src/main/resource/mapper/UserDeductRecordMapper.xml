<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spider.mapper.UserDeductRecordMapper">
    <insert id="addUserDeductRecord">
          insert  into user_deduct_record
          <trim prefix="(" suffix=")" suffixOverrides=",">
 user_deduct_record.task_id,
 user_deduct_record.price,
 user_deduct_record.amount,
 user_deduct_record.deduct_time,

          </trim>
          values
          <trim prefix="(" suffix=")" suffixOverrides=",">
 #{taskId} ,
 #{price} ,
 #{amount} ,
 #{deductTime} ,

          </trim>
    </insert>
    <delete id="deleteUserDeductRecordById">
          delete from user_deduct_record
@where
    </delete>
    <update id="updateUserDeductRecordById">
        update user_deduct_record
@set
@where
    </update>
    <select id="getUserDeductRecordById" resultType="com.spider.pojo.@resultType">
          select
undefined
          form userDeductRecordName

@where
    </select>
    <select id="getUserDeductRecordList" resultType="com.spider.pojo.@resultType">
          select
undefined
          form userDeductRecordName

@where
    </select>

</mapper>