<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spider.mapper.UserChargeRecordMapper">
    <insert id="addUserChargeRecord" parameterType="com.spider.pojo.req.AddUserChargeRecordReq">
          insert  into user_charge_record
          <trim prefix="(" suffix=")" suffixOverrides=",">
             user_charge_record.user_id,
             user_charge_record.way,
             user_charge_record.charge_account,
             user_charge_record.order_id,
             user_charge_record.amount,
             user_charge_record.status,
            <if test="chargeTime != null">
                  user_charge_record.charge_time
            </if>
          </trim>
          values
          <trim prefix="(" suffix=")" suffixOverrides=",">
             #{userId},
             #{way},
             #{chargeAccount},
             #{orderId},
             #{amount},
             #{status},
            <if test="chargeTime != null">
                  #{chargeTime}
            </if>
          </trim>
    </insert>
    <delete id="deleteUserChargeRecordById">
          delete from user_charge_record
          <where>
            <if test="id != null">
                  user_charge_record.id = #{id}
            </if>
          </where>
    </delete>
    <update id="updateUserChargeRecordById" parameterType="com.spider.pojo.param.UpdateUserChargeRecordByIdParams">
        update user_charge_record
        <set>
            <if test="userId != null">
                  user_charge_record.user_id = #{UserId},
            </if>
            <if test="way != null">
                  user_charge_record.way = #{Way},
            </if>
            <if test="chargeAccount != null">
                  user_charge_record.charge_account = #{ChargeAccount},
            </if>
            <if test="orderId != null">
                  user_charge_record.order_id = #{OrderId},
            </if>
            <if test="amount != null">
                  user_charge_record.amount = #{Amount},
            </if>
            <if test="status != null">
                  user_charge_record.status = #{Status},
            </if>
            <if test="chargeTime != null">
                  user_charge_record.charge_time = #{ChargeTime}
            </if>
        </set>
          <where>
            <if test="id != null">
                  user_charge_record.id = #{id}
            </if>
          </where>
    </update>
    <select id="getUserChargeRecordById" resultType="com.spider.pojo.entity.UserChargeRecord">
          select
             user_charge_record.id,
             user_charge_record.user_id,
             user_charge_record.way,
             user_charge_record.charge_account,
             user_charge_record.order_id,
             user_charge_record.amount,
             user_charge_record.status,
             user_charge_record.charge_time
          form user_charge_record
          <where>
            <if test="id != null">
                  user_charge_record.id = #{id}
            </if>
          </where>
    </select>
    <select id="getUserChargeRecordList" resultType="com.spider.pojo.entity.UserChargeRecord" parameterType="com.spider.pojo.req.GetUserChargeRecordListReq">
          select
             user_charge_record.id,
             user_charge_record.user_id,
             user_charge_record.way,
             user_charge_record.charge_account,
             user_charge_record.order_id,
             user_charge_record.amount,
             user_charge_record.status,
             user_charge_record.charge_time
          form user_charge_record
          <where>
            <if test="userId != null">
                  user_charge_record.user_id = #{userId}
            </if>
            <if test="way != null">
                  and user_charge_record.way = #{way}
            </if>
            <if test="chargeAccount != null">
                  and user_charge_record.charge_account = #{chargeAccount}
            </if>
            <if test="orderId != null">
                  and user_charge_record.order_id = #{orderId}
            </if>
            <if test="amount != null">
                  range            </if>
            <if test="status != null">
                  and user_charge_record.status = #{status}
            </if>
            <if test="chargeTime != null">
                  and user_charge_record.charge_time = #{chargeTime}
            </if>
          </where>
    </select>
</mapper>
