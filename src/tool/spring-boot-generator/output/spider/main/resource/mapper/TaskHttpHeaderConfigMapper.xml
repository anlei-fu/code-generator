<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spider.mapper.TaskHttpHeaderConfigMapper">
  <insert id="add">
   insert into @table
   <trim>
   @columns
   <trim>
   values
   <trim>
   @properties
   </trim>
  </insert>
  <delete id="deleteById">
   delete from @table
   where id=#{id}
  </delete>
  <update id="updateById" parameterType="com.spider.pojo.dao.TaskHttpHeaderConfigDao">
    update @table
    set
    <trim suffixOverrides=",">
     @updateItems
    </trim>
    where id = #{id}
  </update>
  <select id="findById">
   select * from @table
   where id=#{id}
  </select>
</mapper>