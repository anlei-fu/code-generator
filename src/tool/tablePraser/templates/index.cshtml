@model @project.name.Model.@table.nameListModel
@{
    Layout = "~/Views/Share;'d/_Layout.cshtml";
}
@s.tag.1@s.tag.2@s.tag.3@s.tag.4
<script type="text/javascript">
 /*
  *
  */
    function MRCenterMsg() {
        this.bind = function () {
            $.paging.config.form = "#conditionForm";
            $.paging.bind();
            $("#ddlps").val($("#ps").val());
            $("#ddlps").change(function () {
                $("#ps").val($(this).val());
            });
            $(".list_submit").click(function () {
                $.paging.submit();
            });
        }
    }

    $(function () {
        var user = new MRCenterMsg();
        user.bind();
    });

@s.script.1 @s.script.2@s.script.3@s.script.4@s.script.5@s.script.6@s.script.7@s.script.8
</script>
<div class="channel_list">
    <form method="get" id="conditionForm">
    <div class="con_search_box clearfix">
         @s.filter.1 @s.filter.2 @s.filter.3 @s.filter.4  @s.filter.5 @s.filter.6 @s.filter.7 @s.filter.8@s.filter.9@s.filter.10
    </div>
    </form>
    <div>
    </div>
    <div class="@showtable" id="divshelflist">
        <table width="100%" border="0" cellpadding="0" cellspacing="0">
            @s.table
        </table>
        <div class="list_number">
            共 @Model.TotalCount 条记录 | 每页显示:
            <select id="ddlps">
                <option value="10">10</option>
                <option value="20">20</option>
                <option value="50">50</option>
                <option value="100">100</option>
            </select>
            条
        </div>
        <div id="pager" class="list_pager">
        </div>
        <div style="clear: both;">
        </div>
    </div>
</div>
