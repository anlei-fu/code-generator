<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="product">
        <sql id="-1">
         <if test="id">
           ${id}
         </if>
           1,2,6
        </sql>
        <select id="0">
        <include refid="product.-1">
     select *from user where 
        </select>

</mapper>